  <div>
  <h1>TOP 25 GAMES</h1>
 {{#each allGames}}
<div id="card-games">
  <div class="card" style="width: 18rem;">
    <img src="{{this.cover}}" class="card-img-top" alt="{{this.title}}" />
    <div class="card-body">
      <h5 class="card-title">{{this.title}}</h5>
      <p class="card-text">RATING: {{this.rating}} </p>
      <a href="/game/{{this._id}}/details" class="btn btn-primary">CHECK GAME</a>
    </div>
  </div>
  {{/each}}
</div>

  </div>

  {{!-- <ul>
    {{#each allGames}}
      <li>
        <a href="/game/{{this._id}}/details">{{this.title}}</a>
      </li>
      <img src="{{this.cover}}" alt="{{this.title}}" width="300px" />
    {{/each}}
  </ul> --}}